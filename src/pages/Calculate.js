import React from 'react';

const Calculate = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Calculate</h1>
    </div>
  );
};

export default Calculate;